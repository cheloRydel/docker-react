sudo: required
language: generic 
services:
  - docker

before_install:
  - docker build -t marcelorydel/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true marcelorydel/docker-react npm run test